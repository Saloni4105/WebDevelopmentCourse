// && -> and (both condition should be true)
//|| -> or {if one condition is true, that is Okay..}
// ! -> not operator or reverse

let isloggedIn= true;
let isPaid= false;

console.log(isloggedIn && isPaid);

let isGmailUser= true;
let isGoogleUser= false;

console.log(isGmailUser || isGoogleUser);
